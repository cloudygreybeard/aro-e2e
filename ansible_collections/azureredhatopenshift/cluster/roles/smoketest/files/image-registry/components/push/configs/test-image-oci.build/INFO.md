### test-image-oci

A deliberately limited Dockerfile and Makefile for creating a reasonably tiny test image (OCI Archive) for distribution in the ARO Smoke Tests for Image Registry
Besides taking up very little room, it just prints `/hello`.
